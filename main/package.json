{
  "name": "main",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "esbuild  ./index.ts --bundle --target=node14 --sourcemap --outfile=../dist/main-bundle.js --external:fs --external:electron --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.14.28"
  }
}
